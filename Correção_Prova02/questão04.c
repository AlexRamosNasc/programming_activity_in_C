#include <stdio.h>
int main(){
  int n, i, x, cont = 0;
  scanf("%d", &n);
  int v[n];
  for(i = 0; i < n; i++){
    scanf("%d", &v[i]);
  }
  scanf("%d", &x);
  for(i = 0; i < n; i++){
    if(i = 0; i < n; i++){
      cont++;
    }
  }
  if(cont > 0){
    printf("%d cadastrado &d vezes", n, cont);
  }
  else{
    printf("%d não cadastrado", x);
  }
  return 0;
}
  
